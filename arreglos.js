const arreglo = ['Texto', 456.1, false, { propiedad:'valor' }, [1, 2, 3]];
console.log(arreglo);

const amigos = ['Kendall', 'Manuel', 'Cesar'];
console.log(amigos[0]);

const colores = [];
colores[0] = 'Rojo';
colores[1] = 'Verde';
colores[3] = 'Blanco';

console.log('El arreglo colores tiene: ' + colores.length + ' colores');

colores.push('Azul');
console.log(colores);